#include <string>
#include <vector>
#include <algorithm>

using namespace std;

vector<string> solution(vector<vector<string>> tickets) {
    vector<string> answer;
    
    vector<vector<<string>>g;//그래프
    for(int i=0; i<tickets.size(); i++){
        
    }
    //알파벳 앞서는 순서로 하기 위해 정렬
    for(int i=0; i<g.size(); i++){
        sort(g[i].begin(), g[i].end());
    }
    
    
    return answer;
}